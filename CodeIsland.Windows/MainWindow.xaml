<Window x:Class="CodeIsland.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CodeIsland.Windows"
        mc:Ignorable="d"
        Title="CodeIsland" Height="160" Width="560"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Border x:Name="PanelBorder" CornerRadius="18" Background="#FF000000" BorderBrush="#FF343A46" BorderThickness="1" Padding="18" RenderTransformOrigin="0.5,0">
        <Border.RenderTransform><ScaleTransform x:Name="PanelScale" ScaleX="1" ScaleY="1"/></Border.RenderTransform>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Width="54" Height="54" CornerRadius="27" Background="#FF30394A" Margin="0,0,14,0">
                <TextBlock Text="CI" Foreground="#FF8BE9FD" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <Grid Grid.Column="1" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Foreground="White" FontSize="18" FontWeight="SemiBold">
                        <Run Text="CodeIsland"/><Run Text="  "/><Run Text="{Binding SessionCount, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock Text="{DynamicResource IdleText}" Foreground="#FFB8C1D1" Margin="0,6,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers><DataTrigger Binding="{Binding IsIdle}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <ItemsControl ItemsSource="{Binding Sessions}" MaxHeight="82">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5,0,0">
                                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="110"/><ColumnDefinition Width="*"/><ColumnDefinition Width="48"/><ColumnDefinition Width="28"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Agent}" Foreground="#FF8BE9FD"/>
                                    <TextBlock Grid.Column="1" Text="{Binding State}" Foreground="#FFF4C86A"/>
                                    <TextBlock Grid.Column="2" Text="{Binding LastMessage}" Foreground="#FFD5DAE4" TextTrimming="CharacterEllipsis"/>
                                    <Button Grid.Column="3" Content="跳转" Tag="{Binding}" Click="OnJumpClick" ToolTip="Activate the matching terminal or IDE window" Padding="4,0" Margin="0,0,4,0"/>
                                    <Button Grid.Column="4" Content="×" Tag="{Binding SessionId}" Click="OnCloseSessionClick" ToolTip="{DynamicResource CloseSessionText}" Width="24" Height="22" Padding="0"/>
                                    <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,7,0,0">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers><DataTrigger Binding="{Binding State}" Value="WaitingForPermission"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <Button Content="{DynamicResource ApproveText}" Tag="{Binding PendingEventId}" Click="OnApproveClick" Padding="10,4" Margin="0,0,6,0"/>
                                        <Button Content="{DynamicResource DenyText}" Tag="{Binding PendingEventId}" Click="OnDenyClick" Padding="10,4" Margin="0,0,6,0"/>
                                        <Button Content="{DynamicResource AlwaysAllowText}" Tag="{Binding PendingEventId}" Click="OnAlwaysAllowClick" Padding="10,4"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,7,0,0">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers><DataTrigger Binding="{Binding State}" Value="WaitingForAnswer"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBox Width="250" Tag="{Binding PendingEventId}" TextChanged="OnAnswerChanged" Padding="6,4"/>
                                        <Button Content="{DynamicResource SendText}" Tag="{Binding PendingEventId}" Click="OnAnswerClick" Padding="12,4" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
